# BuildLoop — Agent Notes

BuildLoop completes bounded software-development tasks autonomously while preserving human control over security-sensitive and irreversible actions.

## Repository

- Primary remote for hackathon submission: `git@github.com:akbartantu/buildloop-hackathon-2026.git`
- Do not force-push published history.
- Keep commits in a working state before pushing.

## Safety

- Never commit `.env`, credentials, or `.buildloop/` runtime state.
- Do not expose secrets in logs, evidence, or client bundles.
